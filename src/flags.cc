#include <gflags/gflags.h>

DEFINE_string(ins_servers, "", "server list of nexus, e.g abc.com:1234,def.com:5342");
DEFINE_string(dqs_root_path, "/dqs", "the root path of dqs");
DEFINE_string(dqs_port, "9527", "the port of dqs");
